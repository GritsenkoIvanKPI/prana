node_modules/
temporary screenshots/
.DS_Store

# design reference screenshots (not part of the site)
2.png
3.png
lunarch.framer.website_.png
mobile.png

# contains a live API key, not needed to run the site
GEMINI_IMAGE_GENERATION.md

# raw source footage (69MB) — compressed web versions live in images/
*.MOV

# raw uncropped source photos handed off for a shoot — cropped/compressed
# copies get placed in images/ under their own descriptive names
new_photo/

# real Telegram bot token/chat id — copy telegram-config.example.php instead
telegram-config.php

# build artifact from make-deploy.sh
prana-deploy.zip
prana-deploy/
